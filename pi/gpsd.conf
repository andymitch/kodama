# gpsd configuration for SIM7600 GPS
# NMEA data is on /dev/ttyUSB1 (second serial port of the SIM7600)
DEVICES="/dev/ttyUSB1"

# -n: don't wait for a client to connect before polling
GPSD_OPTIONS="-n"

# Auto-detect USB GPS devices
USBAUTO="true"
