name: Server Alpha Release

on:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  clean-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Delete existing server-alpha release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete server-alpha --yes 2>/dev/null || true
          git push origin :refs/tags/server-alpha 2>/dev/null || true

  build:
    needs: clean-release
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            target: aarch64-apple-darwin
            archive: tar.gz
          - platform: macos-latest
            target: x86_64-apple-darwin
            archive: tar.gz
          - platform: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            archive: tar.gz
          - platform: windows-latest
            target: x86_64-pc-windows-msvc
            archive: zip

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Pin crypto dependencies
        run: |
          cargo update -p sha2 --precise 0.11.0-rc.4 || cargo update -p sha2@0.11.0-rc.4 --precise 0.11.0-rc.4 || true
          cargo update -p digest --precise 0.11.0-rc.9 || cargo update -p digest@0.11.0-rc.9 --precise 0.11.0-rc.9 || true

      - name: Build
        run: cargo build --release --target ${{ matrix.target }} -p kodama-server

      - name: Package (unix)
        if: matrix.archive == 'tar.gz'
        run: |
          cd target/${{ matrix.target }}/release
          tar czf ../../../kodama-server-${{ matrix.target }}.tar.gz kodama-server
        shell: bash

      - name: Package (windows)
        if: matrix.archive == 'zip'
        run: |
          cd target/${{ matrix.target }}/release
          7z a ../../../kodama-server-${{ matrix.target }}.zip kodama-server.exe
        shell: bash

      - name: Set short SHA
        shell: bash
        run: echo "SHORT_SHA=$(git rev-parse --short HEAD)" >> $GITHUB_ENV

      - name: Upload to release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: server-alpha
          name: "Server Alpha (${{ env.SHORT_SHA }})"
          prerelease: true
          body: |
            Rolling alpha build of `kodama-server` from `main` at ${{ github.sha }}.

            **Downloads:**
            - macOS (Apple Silicon): `kodama-server-aarch64-apple-darwin.tar.gz`
            - macOS (Intel): `kodama-server-x86_64-apple-darwin.tar.gz`
            - Linux (x86_64): `kodama-server-x86_64-unknown-linux-gnu.tar.gz`
            - Windows (x86_64): `kodama-server-x86_64-pc-windows-msvc.zip`
          files: kodama-server-${{ matrix.target }}.${{ matrix.archive }}
